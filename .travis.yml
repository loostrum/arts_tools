dist: xenial   # required for Python >= 3.7
language: python
python:
    - "3.6"
    - "3.7"
install:
    - pip install pycodestyle
    - pip install .
script:
    - find . -name "*.py" -exec pycodestyle --ignore E501 {} +
